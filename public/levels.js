class obstacle{
constructor(x, y, w, h, color ){
this.x = x;
this.y = y;
this.w = w; 
this.h = h; 
this.color = color;
 }
}
class spikes{
constructor(x, y, point, color ){
this.x 
this.y
this.point = abc
this.color = color




    }
}
class dissapear{
    constructor(x, y, w, h, color, hide ){
    //initializing the properties
    //x y w h , block 
    this.x = x;
    this.y = y;
    this.w = w; 
    this.h = h; 
    this.color = color;
    this.hide
     }
    //defining the methods
    //(this) refers to this instance of object
    
    
    
    
    
    }
    class levels{
        constructor(){
        //initializing the properties


         }
        //defining the methods
        //(this) refers to this instance of object
        
        
        
        
        
        }